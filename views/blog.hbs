<div class="row">
    <section class="main col-md-9">
        <article>
            <h2>{{data.post.title}}</h2>
            <img src={{data.post.featureImage}} alt="feature Image" style="width:100%" />
            <br /><br />
            {{#safeHTML data.post.body}}{{/safeHTML}}
            <br /><br />
            <strong>Category:</strong> {{data.post.category}} <br />
            <strong>Last Updated:</strong> {{data.post.postDate}}

        </article>
    </section>
    {{#if data.post}}
    <aside class="col-md-3">
        <br /><br />
        {{#if data.posts}}
        <h4>Posts</h4>
         {{#each data.posts}}
        <ul class="list-group">
            <a href="/blog/id?category={{../viewingCategory}}" class="list-group-item">{{title}}</a>
        </ul>
        {{/each}}
        {{/if}}
        {{#if data.categories}}
        <h4>Categories</h4>
        {{#each data.categories}}

        <ul class="list-group">
           <a href="/blog?category={{data.post.id}}" class="list-group-item">{{category}}</a>
        </ul>
        {{/each}}
        {{/if}}
    </aside>
    {{/if}}
</div>
<div class="col-md-12 text-center">
    <h2>{{data.message}}</h2>
    <p>Please try another Post / Category</p>
</div>
